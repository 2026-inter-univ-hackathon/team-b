// このファイルを js/config.js にコピーして、トークンを貼ってください。
// js/config.js は .gitignore で除外されており、リポジトリには入りません。
// エディタの一覧に config.js が出ないのは、無視ファイルを隠す設定のためです。消えてはいません。
window.APP_CONFIG = {
  // 公共交通オープンデータセンター（https://developer.odpt.org/）で登録すると発行されるアクセストークン。
  // 未設定でも都営線だけは動く（公開ダンプを使う）。
  ODPT_ACCESS_TOKEN: "ここに ODPT のアクセストークンを貼る",
};
