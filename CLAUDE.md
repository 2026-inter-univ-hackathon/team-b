# 積分アラーム

> **14:45 に最初にやること。** 14:15 と 14:45 に Slack へ投稿した内容を、下の「これは何か」と
> 「決めたこと」に**貼るだけ**です。書き直さなくて構いません。

## これは何か

アラームを止めても二度寝してしまう理系大学生が、積分の計算問題を解かないとアラームを止められない仕組みで、朝ちゃんと起きられるようにする

---

## このプロジェクトの制約

**この制約は変更しないでください。**

- **素の HTML / CSS / JavaScript のみ。** ビルドツール・パッケージマネージャ・フレームワークは使わない
- **データは localStorage に保存する。** サーバーやデータベースは作らない
- **`index.html` をブラウザで開けば動くこと。** 開発サーバーを立てる必要がない状態を保つ
- **外部ライブラリは CDN から読み込まない。** 会場のネットワークが不安定なため、オフラインでも動く状態にする
- **主要な動線を1本だけ通す。** ログイン・管理画面・設定画面は作らない
- **最後の判断は人がする形にする。** 全自動にしない。AI が提案し、人が選ぶ／決める

---

## 申し送り（交代のたびに書き換える）

**9/4 の前半（〜16:15）は10分ごとに交代します。そのたびにここを書き換えてください。**
**後半（16:35〜）は交代しません。**
**開発期間（9/5〜9/24）は、次に作業する自分に宛てて書いてください。** 数日空くと忘れます。

- **今動いているもの**: `index.html` 1枚で完結。①時刻をセットする → ②その時刻に鳴って積分の問題が3つ候補で出る → ③1つ選んで答えを入力すると、合っていればアラームが止まる、まで一通り動く。音は Web Audio API（外部ファイルなし）。テスト用に「今すぐ鳴らす」ボタンあり
- **今やりかけのこと**: 特になし（一通り動く状態）
- **次にやること**: 実機・別ブラウザでの動作確認、問題の難易度調整（簡単すぎ／難しすぎないか）、見た目の調整
- **ハマっていること**: 特になし。ブラウザの自動再生制限で音が鳴らない場合は、先に「セットする」か「今すぐ鳴らす」ボタンを押す（＝ユーザー操作の直後に音を開始する）ことで回避している

---

## 決めたこと（消さずに追記する）

チームで決めたことをここに足していく。ドライバーが替わっても設計がぶれないようにするため。

**14:45 の投稿の「今日は作らないもの」も、ここに書き写してください。** 開発中に蒸し返さないためです。

- 保存するデータは `localStorage` のキー `calc-alarm-data` にまとめる（アラーム時刻とセット状態だけ）
- 画面は `index.html` の1枚だけにする
- 積分の問題は定積分（数値で答えが決まる形）だけ出す。不定積分（+C が必要な形）は出さない
- 正解の判定は誤差 0.05 以内の数値一致で行う
- 問題は「簡単・普通・難しい」の3つを毎回ランダム生成し、その中から人が1つ選んで解く
- 正解してアラームが止まったら自動では再セットしない。次の朝はまた自分でセットする
- 今日は作らないもの: ログイン、アラーム音源のカスタマイズ、履歴・統計画面、通知機能、スヌーズ機能

---

## AI に任せない部分

このハッカソンの共通制約です。**最後の判断を人が持つ場所**を1つ決めて、ここに書いてください。
発表で「AI に足りないのは『　』だ」を述べるときの根拠になります。

- **人が判断する場所**: アラームが鳴ったとき、どの問題を解くか（簡単・普通・難しい）を選ぶのは必ず人。AI（アプリ）は3つ候補を出すだけで、自動では選ばない。また、答えが合っているかを最終的に確かめて次の一歩（起きる）に進むのも人
- **その理由**: 難易度の選択を自動化すると、眠くて簡単な問題ばかり選ぶ・逆に無理な難易度を機械的に割り当ててしまう、といったズレが起きる。何より「解けた＝起きた」を確認できるのは本人だけなので、判定の最後の一手は人に残す
