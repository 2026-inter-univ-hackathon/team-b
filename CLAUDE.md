# （プロダクト名をここに書く）

> **14:45 に最初にやること。** 14:15 と 14:45 に Slack へ投稿した内容を、下の「これは何か」と
> 「決めたこと」に**貼るだけ**です。書き直さなくて構いません。

## これは何か

理系大学生が、程よい難易度の理系問題を解かないと止められないアラーム

---

## このプロジェクトの制約

**この制約は変更しないでください。**

- **素の HTML / CSS / JavaScript のみ。** ビルドツール・パッケージマネージャ・フレームワークは使わない
- **データは localStorage に保存する。** サーバーやデータベースは作らない
- **`index.html` をブラウザで開けば動くこと。** 開発サーバーを立てる必要がない状態を保つ
- **外部ライブラリは CDN から読み込まない。** 会場のネットワークが不安定なため、オフラインでも動く状態にする
- **主要な動線を1本だけ通す。** ログイン・管理画面・設定画面は作らない
- **最後の判断は人がする形にする。** 全自動にしない。AI が提案し、人が選ぶ／決める

---

## 申し送り（交代のたびに書き換える）

**9/4 の前半（〜16:15）は10分ごとに交代します。そのたびにここを書き換えてください。**
**後半（16:35〜）は交代しません。**
**開発期間（9/5〜9/24）は、次に作業する自分に宛てて書いてください。** 数日空くと忘れます。

- **今動いているもの**: アラーム時刻をセットする→時刻になると音が鳴り理系問題が出る→正解すると音が止まる、の一連の動線
- **今やりかけのこと**: なし（一旦の実装は完了）
- **次にやること**: 実機・複数ブラウザでの動作確認、問題のバリエーション追加（時間が余れば）
- **ハマっていること**: 特になし

---

## 決めたこと（消さずに追記する）

チームで決めたことをここに足していく。ドライバーが替わっても設計がぶれないようにするため。

**14:45 の投稿の「今日は作らないもの」も、ここに書き写してください。** 開発中に蒸し返さないためです。

- 保存するデータは `localStorage` のキー `app-data` にまとめる（アラーム時刻のみ）
- 画面は `index.html` の1枚だけにする
- アラームは同時に1つだけ設定できるようにする（複数アラームの管理画面は作らない）
- 問題は毎回ランダムな数値で作り直す。不正解のときは別の問題を出す
- 正解したらアラームは自動的に解除される（一回きり。繰り返し設定は作らない）
- アラーム音は外部音源を使わず、Web Audio API でその場で鳴らす
- 今日は通知機能・スヌーズ機能・ログイン・設定画面を作らない

---

## AI に任せない部分

このハッカソンの共通制約です。**最後の判断を人が持つ場所**を1つ決めて、ここに書いてください。
発表で「AI に足りないのは『　』だ」を述べるときの根拠になります。

- **人が判断する場所**: 出題された問題を実際に解いて、答えを入力する部分。正誤判定はアプリが自動で行うが、解答そのものは必ず人間が考えて入力する（AI が代わりに解く・スキップする・自動で止める機能は作らない）
- **その理由**: 「解けたら止まる」という体験の価値は、人が実際に頭を使って考える行為そのものにある。ここを自動化すると、アラームとして機能しなくなる
