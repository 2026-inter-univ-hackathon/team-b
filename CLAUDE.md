# 絶起ヘルパー

> **14:45 に最初にやること。** 14:15 と 14:45 に Slack へ投稿した内容を、下の「これは何か」と
> 「決めたこと」に**貼るだけ**です。書き直さなくて構いません。

## これは何か

（1行。誰の何を解決するか。**14:15 の投稿の①②**をそのまま）

---

## このプロジェクトの制約

**この制約は変更しないでください。**

- **素の HTML / CSS / JavaScript のみ。** ビルドツール・パッケージマネージャ・フレームワークは使わない
- **データは localStorage に保存する。** サーバーやデータベースは作らない
- **`index.html` をブラウザで開けば動くこと。** 開発サーバーを立てる必要がない状態を保つ
- **外部ライブラリは CDN から読み込まない。** 会場のネットワークが不安定なため、オフラインでも動く状態にする
- **主要な動線を1本だけ通す。** ログイン・管理画面・設定画面は作らない
- **最後の判断は人がする形にする。** 全自動にしない。AI が提案し、人が選ぶ／決める

---

## 申し送り（交代のたびに書き換える）

**9/4 の前半（〜16:15）は10分ごとに交代します。そのたびにここを書き換えてください。**
**後半（16:35〜）は交代しません。**
**開発期間（9/5〜9/24）は、次に作業する自分に宛てて書いてください。** 数日空くと忘れます。

- **今動いているもの**: 主要動線が一通り動く。セット → 監視（カウントダウン）→ 鳴動（問題＋音）→ 正解で停止 → ログ表示。localStorage 保存とリロード復元も動く。ジャンル選択（数学／物理／プログラミング）と難易度選択（やさしい／ふつう／むずかしい）と音の選択（ビープ／サイレン／学校のチャイム／目覚まし時計、試聴つき）あり。出題は 15 種類（9/9 に 9 種類追加）
- **今やりかけのこと**: 難易度選択と音の選択を実装したがブラウザでの通しデモは未確認（9/9）。F-09（起床ログ）まで実装済み。追加で「5分正解できなければ難易度を下げる（ログに残す）」「音量を30秒ごとに段階的に上げる」「鳴動中のタブタイトル点滅＋通知」「スリープ復帰やリロードで設定時刻を過ぎていたら『今すぐ鳴らす／明日にする』を人に選ばせる」を実装済み
- **次にやること**: file:// で Chrome / Safari 両方の通しデモ。見た目の微調整。README を埋める。「AI に任せない部分」を決めて下に書く
- **終電アラート（9/10）**: 実ダイヤの終電が出せる API は無かった（駅すぱあとフリー＝ダイヤ探索不可、駅すぱあと評価版＝会社名必須・公開不可、NAVITIME RapidAPI＝平均所要時間のみ、Google Routes＝日本の列車なし、ジョルダン オープン API＝時刻表非考慮・直接接続不可。**再検討しない**）。公共交通オープンデータセンター（ODPT）の駅並び順＋列車時刻表で乗り換え 1 回までの終電を `js/odpt.js` で自前探索する形にした。`js/config.js` に ODPT のトークンを貼れば首都圏の JR・メトロ・私鉄、無ければ都営 6 路線のみ（公開ダンプ）。Node での検証は都営分で通っている。トークン取得後に JR・メトロで通しデモが必要
- **ハマっていること**: リロード後は AudioContext が作れないので、監視画面の「音を有効にする」を押してもらう必要がある（自動再生制限）。通知（Notification API）は file:// で権限が取れない場合があるが、タイトル点滅があるので必須ではない

---

## 決めたこと（消さずに追記する）

チームで決めたことをここに足していく。ドライバーが替わっても設計がぶれないようにするため。

**14:45 の投稿の「今日は作らないもの」も、ここに書き写してください。** 開発中に蒸し返さないためです。

- （例）保存するデータは `localStorage` のキー `app-data` にまとめる
- （例）画面は `index.html` の1枚だけにする
- （例）今日は通知機能を作らない

---

## AI に任せない部分

このハッカソンの共通制約です。**最後の判断を人が持つ場所**を1つ決めて、ここに書いてください。
発表で「AI に足りないのは『　』だ」を述べるときの根拠になります。

- **人が判断する場所**:
- **その理由**:
